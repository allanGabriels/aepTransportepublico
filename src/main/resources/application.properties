# Nome da Aplica��o
spring.application.name=aepTransportePublico

# --- Configura��o do Banco de Dados SQLite ---

# URL de conex�o: aponta para o arquivo .db na raiz do projeto
spring.datasource.url=jdbc:sqlite:aepTransportePublico.db

# Driver do SQLite que voc� j� tem no pom.xml (ESSA LINHA � CRUCIAL)
spring.datasource.driver-class-name=org.sqlite.JDBC

# Dialeto para o Hibernate entender os comandos espec�ficos do SQLite
spring.jpa.database-platform=org.hibernate.community.dialect.SQLiteDialect

# Esta � a "m�gica" que cria/atualiza as tabelas automaticamente
spring.jpa.hibernate.ddl-auto=update